# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 本项目从 **JevBystander** 的读屏底座拆出来（同一套无障碍读取与判读口径），
> 加上聊天模型、攻略度与本地记忆。JevBystander 自己的版本记录在它的仓库里。

## [1.2.0] - 2026-09-23

这一版只动**外观与手感**：整套界面换成 Material 3，跟着系统的深浅色和壁纸取色走。

### 新增

- **整套 Material 3 界面**（`com.google.android.material:material:1.12.0` + AppCompat）
  - 设置页：卡片按色阶分层（surface-container 系列）、开关换成 MD3 开关、输入框换成描边盒
    （`TextInputLayout` + `TextInputEditText`）、下拉换成暴露式菜单（`MaterialAutoCompleteTextView`）
  - 结果页：攻略度给到 52sp 大字（配同色进度条，颜色随分数档位变），
    3 条文案各自一张可点卡片 —— 用 `MaterialCardView` 是为了白拿涟漪反馈，
    "点了没反应"是这类复制型界面最让人不放心的地方
  - 常驻浮条：改成**胶囊形**，左侧一个分数色环（≥70 绿 / ≥40 黄 / 其余红）
  - App 图标重画：描边聊天气泡 + 上升箭头，含单色层（Android 13+ 主题图标会自己上色）
- **跟随壁纸取色（Material You）**：`DynamicColors.applyToActivitiesIfAvailable()`；
  界面颜色一律走主题属性（`colorPrimary`、`colorSurfaceContainerHigh`…）而不是写死资源色 ——
  写死的话，系统取色再好看也传不到界面上
- **深色模式**：新增 `values-night` 一套配色；语义色（好/中/差三档）浅深各一版，
  深色下用更亮的一档，否则在深底上发闷

### 修复（改造过程中踩到的坑）

- 下拉控件原先用 `Spinner`：它会在**页面刚建好时就回调一次**（初始选中项）。
  如果回调里做"选中项与当前配置不一致 → 重建页面"，就会变成死循环。
  换成暴露式菜单后只在用户真选时回调，`Providers.isSameAs()` 那层幂等判断继续留着当双保险
- Material 1.12 里**没有** `materialButtonTonalStyle` 这个属性（编译期就报 unresolved）。
  tonal 按钮改成自己染色：`colorSecondaryContainer` + `colorOnSecondaryContainer`
- `boxCornerRadiusTopEnd / BottomStart / BottomEnd` 的 setter 在 1.12 已被移除
  （写成属性会报 `'val' cannot be reassigned`），改用一次设四个角的 `setBoxCornerRadii()`
- 悬浮条是在**无障碍服务的 context** 里造 View 的，Material 主题不在那个 context 上，
  必须先套一层 `ContextThemeWrapper`，否则取主题属性全是系统默认值、颜色全错

### 体积（说清楚，不藏）

- debug 包 **1.1 MB → 6.0 MB**（Material + AppCompat 的 dex 与资源，且 release 也暂不混淆）
- 为什么不顺手开 R8：Material 控件靠**反射**按类名从主题/XML inflate，R8 裁错一个类，
  只有真机点到那一屏才崩，单测完全看不出来。当前没有真机回归条件，
  宁可胖一点，也不发"装得上但某屏必崩"的包（`app/build.gradle.kts` 里有同样的注释）
- 想瘦身：先在**有真机**的环境把设置页全部控件、结果页、悬浮条、图标各走一遍手工回归，
  再把 `isMinifyEnabled` / `isShrinkResources` 打开

### 测试

- 102 个单测全绿（`ProvidersTest` 12 个、`VisionReaderTest` 9 个、`ScoreOverlayTest` 5 个在内）
- ⚠ 这一版的**界面改动尚未在真机上验证**（截稿时手机没连着）：编译与单测都过，
  但"MD3 控件在真机上的实际观感"还需要装一次机确认

## [1.1.0] - 2026-09-23

这一版是被**真机实测**逼出来的：无障碍读微信在新版微信上彻底失效，于是多了两条路。

### 新增

- **内置模型厂商预设**（国内 7 家 + 自定义）：选一家就自动填好**地址 + 聊天模型 + 视觉模型**，
  用户只需要粘密钥。之前得自己知道"地址要写到 `/v1`、模型名怎么拼"，大部分人卡在第一步
  - DeepSeek / 智谱 GLM / 硅基流动 / 阿里云百炼 / 月之暗面 Kimi / 火山方舟 / MiniMax
  - 预设里标出**哪家能看图**（视觉读屏要用），选择列表直接显示"（不支持看图）"，免得选了 DeepSeek 才发现读不了图
  - 火山方舟特别注明：模型名是**自己创建的推理接入点 ID**（`ep-…`），不填会报模型不存在
  - `Providers.detectByUrl()` 从已存地址反推是哪一家；认不出来（自建/中转站）时界面明说
    "不属于任何内置预设"，不会假装是某一家。按**域名边界**匹配，`api.deepseek.com.evil.com` 不会被误认
  - 换厂商**不清空已粘好的密钥**（万一是同一家的第二个账号），只提示"记得换成这一家的"
  - 邀请码/推广链接集中在 `Aff.kt`：留空＝官方入口；设置页明写"作者得少量奖励、不影响你的价格与权益"
- **视觉读屏（截图 → 视觉模型念成文字）**：微信读不到无障碍树时的替代方案
  - 链路：`AccessibilityService.takeScreenshot()` → 缩到 900 宽压 JPEG → 发给看得懂图的模型
    → 拿回 `{"title":…,"messages":[{"mine":…,"text":…}]}` → 后面判读/攻略度/文案**完全不用改**
  - 提示词钉死几条：右侧气泡=我 / 左侧=对方、**原样抄写**不许改写、图片语音用 `[图片]` `[语音]` 占位、输入框草稿不算消息
  - 解析器宽容到底：去 ``` 代码块、抠第一个 `{` 到最后一个 `}`、`mine` 认 `true/"true"/1/是`、脏输入返回 null 而不抛异常
  - 截图会先缩图：1080×2376 的 PNG 是 130KB+ 的 base64，缩到 900 宽 JPEG80 只要约 1/5
  - 实测：一加 Ace 3 Pro 上 8.2 秒，把「文件传输助手」里的「明天有空吗？想请你吃个饭」一字不差读了出来
- **读取方式开关**：`自动`（先试免费的无障碍树，读空了才走视觉）/ `只用无障碍树` / `只用视觉读屏`
- **视觉读屏可单独配端点**（地址 / 密钥 / 模型），留空则跟聊天模型共用一套
- **常驻悬浮条**：把攻略度**一直挂在屏幕上**，而不是弹个窗看一眼就没了
  - 用 `TYPE_ACCESSIBILITY_OVERLAY` 画浮层 —— **不需要**「显示在其他应用上层」权限
  - 拖动挪位置（位置记住）｜点一下打开结果页看 3 条文案（还没结果时点一下就是判读一次）｜长按隐藏
  - 服务重启后按上次的文字与分数原样恢复，不用等下一次判读
  - 颜色跟结果页一致：≥70 绿 / ≥40 黄 / 其余红
- **判读完不再默认弹结果页**（`判读完自动弹结果页` 默认关）：攻略度已经在浮条上，要看文案点浮条

### 修复

- **结果页叠成好几层**：结果页是对话框样式，`singleTop` 挡不住"服务用 `NEW_TASK` 拉起"这条路径的叠加 ——
  真机 `dumpsys` 数出来本包挂了 6 个窗口（浮条 1 + `ResultActivity` **4** + 设置页 1），
  用户看到的是"怎么有两个浮窗"。改成 `launchMode="singleInstance"` + `onNewIntent` 里 `recreate()` 复用同一窗口；
  结果页打开期间用 `ScoreOverlay.setSuppressed()` 把浮条收起来，一次只给一个浮窗（切回微信浮条自动回来）
- **屏幕上多一个系统悬浮圆钮**：配置里的 `flagRequestAccessibilityButton` 会让**系统**自己画一个
  「无障碍快捷按钮」（ColorOS 上是右侧一个球），点一下也触发判读 —— 跟注不注册回调无关
  （`dumpsys` 里 `requestA11yBtn=true`）。静态配置去掉该标志位，改为设置项 + `setServiceInfo()` 运行时可控，默认关
- **明文 HTTP 被系统拦**：自建/自托管的 OpenAI 兼容端点常常只有 HTTP，而 targetSdk ≥ 28 默认禁止明文，
  报错还很难懂（`CLEARTEXT communication not permitted`）。加了 `network_security_config`：
  默认仍强制 HTTPS，只对自建节点 IP（n4 / n1 / localhost / 10.0.2.2）开白名单
- `ScoreOverlay` 的主线程 Handler 改成 `by lazy`：否则纯 JVM 单测一加载这个类就 `NoClassDefFoundError`
  （`format()` / `colorOf()` 是纯逻辑，必须能在没 Android 环境时跑）

### 实测结论：微信 8.0.76 屏蔽了无障碍树

| 对象 | 带文字的节点数 |
|---|---|
| 微信聊天页 `ChattingUI` | **0** |
| 微信搜索页 `FTSMainUI` | **0** |
| 系统自带 `uiautomator` 抓微信 | **0**（407 字节空树） |
| 系统设置 App | 15 |
| 桌面（本服务） | 280 行 |

系统自己的工具都读不到 —— 所以不是本 App 的 bug，**任何**无障碍客户端在新版微信上都读不到聊天内容
（旁白者 JevBystander 同样受影响）。截屏没有被 `FLAG_SECURE` 挡住，所以视觉那条路走得通。

### 测试

- **102 个单测全绿**（新增 `ProvidersTest` 12 个 + `VisionReaderTest` 9 个 + `ScoreOverlayTest` 5 个）

## [1.0.0] - 2026-09-23

### 新增

- **攻略度评分**：让 Jev 就「当前关系进展」给一个 0~100% 的分，结果里显示与上次的差值（`65%（↑ +8）`）。
  - 用 Jev 的 `score` 题型：档位文字 `0%`…`90%` 共 **10 档**，返回值是 `0~9` 的连续浮点，按 `×100/9` 换算成百分比
  - **为什么不是 11 档**：接口硬性上限就是 10 档，用 11 档（`0%`…`100%`）实测直接被拒 ——
    `HTTP 400 {"detail": "Too many score levels. Must have at most 10 levels."}`
  - 精度靠浮点补足：返回 `6.48` → `72%`；`0` → `0%`、`9` → `100%` 两端都取得到
- **3 条候选回复文案**：由**独立的聊天模型**（任何 OpenAI 兼容端点，默认 DeepSeek）生成，不是建议而是成品。
  - 三种风格固定：①**稳妥**（顺着接住、不冒险）②**推进**（给方案/给下一步）③**有趣**（带点玩笑不越界）
  - 提示词里塞进 Jev 的判读结论（含回复姿态）与该联系人的记忆，所以称呼、亲疏、语气都跟着关系走
  - 解析器宽容：`---` 分隔 → `【标题】`行 → 行首编号 → 整段兜底；垃圾输入不抛异常
- **每联系人本地记忆**：`filesDir/memory/<key 的 SHA-1>.json` + `index.json`
  - 滚动摘要（≤600 字）+ 关键事实（≤12 条）+ 历次攻略度（≤50 次）+ 近期对话（≤40 条）
  - 作为「背景」拼进 Jev 的 state，同时也喂给聊天模型 —— 所以它记得「她养了只叫团子的猫」
  - 攒够 N 条新对话后异步压缩一次摘要（默认 8 条，可关）；水位线 `turnsAtSummary` 防止同一批对话被反复压缩
- **结果页**：攻略度（带进度条与涨跌）+ Jev 判读行 + 可点选复制的文案卡片 + 「复制全部」「重新生成」「关闭」
  - 「重新生成」只重打聊天模型，不动 Jev 的判读与攻略度，省一次 Jev 调用
  - 结果页拉不起来时（后台启动被系统拦）自动退化成一条可展开的通知，结果不会丢
- **独立的聊天模型配置**：地址（要填到 `/v1`）、密钥、模型名，与 Jev 的密钥分开存；带「测试聊天模型」按钮

### 变更（相对 JevBystander）

- 包名换成 `io.github.nisaka520.jevguide`，App 名 `Jev攻略`，磁贴 `攻略一下`
- 判读题从 7 个变 8 个（多一个攻略度），`Prompt.requestJson(..., guide = false)` 可退回 7 题省 token
- 结果默认走**结果页**而不是 3 条 Toast（设置里可切回「只弹提示」）
- 设置页新增「聊天模型」「攻略度与记忆」两节，状态栏显示两个密钥与记忆份数

### 修复

- 提示词里「【记忆】」抬头出现两次（`Memories.contextBlock` 自带一个，`ReplyPrompt` 又套了一个）——
  现在会先剥掉再补，并有单测锁住"只出现一次"

### 测试

- **75 个单测全绿**（`gradlew testDebugUnitTest`），纯逻辑、不依赖设备：
  读屏规则、state 拼装、Jev 响应解析、攻略度换算、文案解析、记忆序列化与容错、提示词硬约束

### 实测（真接口 / 真模型）

- 攻略度在三种关系下的区分度（同一段提示词打真实 Jev）：

  | 场景 | 关系 | 最近对话 | 攻略度 |
  |---|---|---|---|
  | 冷场型 | 普通朋友 | 「在吗」→「嗯」→「最近在忙吗」→「还行」 | 48% |
  | 热络型 | 情侣 | 对方主动问下班时间、要来接、约好去她念叨的店 | 87% |
  | 中性型 | 同事 | 报表往来 | 51% |

- 文案格式在真模型上稳定：`gpt-5.6-luna` 与 `gpt-oss-120b` 都输出 3 段（`【稳妥】/【推进】/【有趣】` + `---`），
  且都用上了记忆里的「团子」
